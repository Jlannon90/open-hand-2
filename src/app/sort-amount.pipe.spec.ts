import { SortAmountPipe } from './sort-amount.pipe';

describe('SortAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
